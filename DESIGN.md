# 育成リソースノート DESIGN

このファイルはアプリ全体のUIデザインルールを定義する。
UIを変更・追加するときは、このルールを基準にする。

機能・データ・動作仕様については SPEC.md を優先する。


## 1. デザイン方針

- iPhoneでの使用を最優先する。
- ミニマルで完成度の高いデータ管理アプリを目指す。
- 装飾を増やすのではなく、余白・整列・文字・情報階層で見やすくする。
- 色数を増やしすぎない。
- カードや枠を必要以上に使用しない。
- 同じ役割のUIは全画面で統一する。
- 重要な情報ほど大きく・濃く表示する。
- 補足情報は主情報より控えめにする。
- スマホで縦に長くなりすぎないよう、情報密度にも配慮する。


## 2. 基準画面

記録タブの現在のデザインを、アプリ全体の基準デザインとする。

他のタブは記録タブをそのままコピーするのではなく、
各画面の目的に合わせながら、同じデザインルールを使用する。

記録画面：
入力・記録操作を主役にする。

集計画面：
合計値・内訳・グラフなどのデータを主役にする。

履歴画面：
日時・種類・内容を素早く判別できることを優先する。


## 3. 色

基本色は現在の記録タブで使用しているCSS変数・色を基準とする。

- 背景：薄いオフホワイト系
- 情報面・カード：白系
- 主文字：濃いニュートラルカラー
- 補助文字：主文字より薄いグレー系
- 区切り線：薄いニュートラルカラー
- アクセント：現在の記録タブのアクセントカラー

アメ拾い・ミュウ・デリバードなどの色は、
必要な識別にのみ使用する。

大きな面を色で塗り分けるのではなく、
見出しのアクセントなど小さな範囲で使用する。

色だけに意味を持たせず、必ず文字でも内容を判別できるようにする。


## 4. 文字と情報階層

目安：

- 合計など最重要の数字：36〜40px / 700
- 画面タイトル：22px / 700
- セクション見出し：17px前後 / 600
- 主要操作・入力値：16px以上
- 内訳の重要な数字：18px前後 / 600
- 本文・項目名：14〜15px
- 補足・単位：12〜13px

主要情報に10〜11pxなど極端に小さい文字を使用しない。

情報は基本的に、

見出し
→ 重要情報
→ 操作
→ 補足

の順に視線が流れるようにする。


## 5. 数字

このアプリでは数字を重要な情報として扱う。

- 合計値は画面内で最も強く表示する。
- 同じ種類の数字は列位置を揃える。
- 表形式の数値は右揃えを基本とする。
- 数字と単位は分けて見せる。
- 単位は数字より小さくする。
- 大きな数字には3桁ごとのカンマを表示する。
- 回数・個数など複数の数値を文章の中に詰め込まず、可能なら列で整列する。


## 6. 余白

4px刻みを基本とする。

目安：

- 画面左右：16px
- カード内：16px
- セクション間：20〜24px
- 項目間：8〜12px
- ラベルと入力欄：6〜8px
- 数字と単位：2〜4px

同じ情報グループ内は近づけ、
別の情報グループとの間には明確な余白を設ける。

不要な余白によって縦スクロールを増やさない。


## 7. カード

カードは以下のような情報のまとまりに使用する。

- 合計
- まとまった入力フォーム
- グラフ
- 分析結果

すべての情報をカード化しない。

- カード背景は白系を基本とする。
- 強い影は使用しない。
- 必要に応じて薄い枠線で区切る。
- 角丸は現在の記録タブを基準に統一する。
- カード内にさらに不要なカードを重ねない。


## 8. セクション見出し

記録タブで使用している、

「細いアクセント＋見出し」

のデザインを基本とする。

同じ階層の見出しは、
文字サイズ・太さ・余白を統一する。


## 9. ボタン

ボタンは役割によって強弱を付ける。

主操作：
- 記録
- 保存
- 決定

→ 最も分かりやすくする。

補助操作：
- 今日
- 画像作成
- 開閉
- キャンセル

→ 主操作より控えめにする。

危険操作：
- 削除

→ 赤系の文字などで意味を示すが、通常時に過度に目立たせない。

タップ領域は44px以上を基本とする。

同じ役割のボタンは、
高さ・角丸・文字サイズを統一する。


## 10. 入力欄

- 入力値は16px以上を基本とする。
- 枠線は控えめにする。
- フォーカス時は操作中であることが分かるようにする。
- 数量は右揃えを基本とする。
- 名前・メモなどの文章は左揃えにする。
- エラーや補足は入力欄との関係が分かる位置に表示する。
- 入力欄を必要以上に大きくしない。


## 11. 集計画面

アメ・ゆめのかけらなどの集計画面では、

1. 選択期間
2. 合計
3. 内訳
4. グラフ・詳細分析

の関係が一目で分かるようにする。

- 合計数字を最も目立たせる。
- 内訳の数値を整列させる。
- 期間選択UIはデータより目立たせない。
- 表やグラフをスマホ幅からはみ出させない。
- グラフの装飾よりデータを優先する。
- セクションごとに不要な外枠を追加しない。


## 12. タブ

- 6タブを同じ幅・高さで扱う。
- 選択中のタブを明確にする。
- 未選択タブは目立たせすぎない。
- タブを画面内の主要操作より強くしない。
- 「ゆめのかけら」は以下の2行表示を維持する。

  ゆめの
  かけら

- 下部の安全領域を確保する。


## 13. スマホ対応

390px前後のiPhone幅を基準に確認する。

変更後は最低限、

- 横スクロールが発生していない
- 文字が不自然に折り返されていない
- ボタンが押しにくくない
- 数字の列が揃っている
- 長い名前でもレイアウトが崩れない
- 大きな数値でもはみ出さない
- 下部タブにコンテンツが隠れない

ことを確認する。


## 14. UI変更時のルール

新しいUIを追加するときは、
新しいデザインをその場で作るのではなく、
既存の同じ役割のUIを再利用する。

既存ルールで対応できない場合のみ、
新しいデザインルールを追加する。

一画面だけ特別なCSSを増やすより、
可能な範囲で共通スタイルを使用する。

## 15. 現在の記録タブの実装値（具体的な基準）

この節は `index.html` の読み込み順（`style.css` → `record-design.css`）と、
CSSの詳細度・後続の上書きを確認して記載した実装値である。
上記の「目安」と実装値が異なる場合も、ここでは実装値を記録する。
新しいデザインへの変更指示ではなく、確定済みの記録タブを参照するための資料とする。
数値はCSS上の値であり、実機での計測値ではない。

### 15.1 色（第3節の具体値）

| 用途 | 現在の値 | 適用箇所・注意 |
|---|---|---|
| ページ背景 | `#fbf8ee` | `style.css` の `:root` から継承。記録日時の帯も同色 |
| 情報面・カード・入力背景 | `#ffffff` | 記録タブの通常カード、合計、ポケモン枠、入力欄 |
| 主文字 | `#292a26` | `#record` と各取得元の本文 |
| 補助文字 | `#686b63` | ラベル、補足、内訳見出し・単位 |
| アクセント | `#756027` | `--accent`。主操作の背景・枠線にも使用 |
| 区切り・通常枠 | `#e4e3da` | `--edge` |
| 控えめな操作背景 | `#f4f0e4` | `--tint`、ミュウ・かけらの数量ボタン |
| 数量ボタン文字 | `#564721` | ミュウ・かけらの数量ボタン |
| 入力枠線 | `#d3d5ca` | 数値・選択入力、日付入力枠 |
| 通常ボタン押下時 | `#e9e2cd` | 有効なボタンの押下中 |
| 主操作押下時 | `#59491e` | `type=submit` の押下中。文字は白 |
| 無効なボタン | 文字 `#8a8d84`／背景 `#f1f1eb`／枠 `#e8e8e0` | 不透明度は `1` |
| R枠 | 枠 `#a89461`／背景 `#fffdf7` | 枠は1px、影なし |
| アメ拾いの目印 | `#567563` | セクション見出し前の小さな線 |
| ミュウの目印 | `#a46f85` | 同上 |
| デリバードの目印 | `#ae7568` | 同上 |

取得元ごとの広い色面は使わず、文字色・操作色は共通化している。

### 15.2 文字・数字（第4・5節の具体値）

| 要素 | サイズ／太さ | 補足 |
|---|---|---|
| 合計数字 | `38px / 700` | 行高 `1.15`、字間 `-1px` |
| 合計の単位 | `15px / 400` | 字間 `0`、数字との左余白 `4px` |
| セクション見出し | `17px / 600` | 行高 `1.5` |
| 入力カード内の見出し | `16px / 600` | `h3` |
| ポケモン枠の名前 | `15px / 600` | 行高 `1.5` |
| ニックネーム | 親文字の `0.82em` | 太さは継承。括弧付き表記は維持 |
| 本文・ラベル | `14px` | 行高 `1.5` |
| カード直下の補足文 | `13px` | 通常の段落より小さくする |
| 内訳数字 | `18px / 600` | 桁幅を揃える `tabular-nums` |
| 内訳項目名 | `14px / 400` | 左揃え。回数・アメ数は右揃え |
| 内訳列見出し・単位 | `12px / 400` | 単位の左余白は `1px` |
| 記録日時ボタン | `16px / 600` | 字間 `-0.4px`、1行表示 |
| 「今すぐ記録」ラベル | `12px` | 文字サイズとタップ領域は別に確保 |
| ミュウのスキル行 | `14px / 500` | 下余白 `4px` |
| ミュウの補足行 | `12px` | 行高 `1.5`、下余白 `12px` |
| 数値・選択入力 | `16px` | 数量は右揃え |

フォントは `-apple-system, BlinkMacSystemFont, "Hiragino Kaku Gothic ProN", sans-serif`。
ミュウは「名前 → スキル → Lv・確率・イベント補正」の順。

### 15.3 角丸・枠・影（第7・8節の具体値）

- 通常カード・合計カード：角丸 `16px`、枠線なし、影なし。
- ポケモン枠・ボタン・入力欄：角丸 `10px`。
- ポケモン枠：通常 `1px solid #e4e3da`、R枠のみ上記の専用色。
- sticky日時の外側の帯：角丸 `0`、下端の区切り線 `1px`。日時入力面自体は角丸 `10px`。
- セクション見出しの目印：幅 `4px` × 高さ `14px`、角丸 `2px`、文字との間隔 `8px`。
- フォーカス表示：共通CSSの `3px solid var(--accent)`、外側への間隔 `2px`。

### 15.4 主要な余白・寸法（第6・9・10節の具体値）

上下・左右の順で表記する。最小高さは固定高さではなく、長い名前などで必要に応じて伸びる。

| 対象 | 現在の値 |
|---|---|
| ページ左右余白 | 幅400px以下は `12px`（390px時も同じ）、それより広い場合は `18px` |
| ページ上下余白 | 上 `24px`、下 `110px + safe-area-inset-bottom` |
| 通常カード内 | `16px`、カード下余白 `16px` |
| 今日の合計カード内 | 上下 `12px`、左右 `20px` |
| 合計数字の外側余白 | 上 `4px`、下 `8px` |
| 合計内訳のセル | 上下 `6px`。見出し行のみ上 `0`・下 `4px` |
| 合計内訳の列間 | セル左余白 `8px`、先頭列は `0`。360px以下では `5px` |
| 記録セクション見出し | 上 `20px`、下 `12px`。ミュウのみ下 `6px` |
| 記録セクションの枠一覧 | 下余白 `16px`（後続指定が以前の20px・24pxを上書き） |
| 通常の2列枠一覧 | 列・行の間隔 `8px` |
| アメ拾いの2列枠一覧 | 列・行の間隔 `6px` |
| 通常のポケモン枠 | 最小高さ `88px`、内側 `12px`、内部要素間 `6px` |
| アメ拾いのポケモン枠 | 最小高さ `76px`、内側上下 `10px`・左右 `12px`、内部要素間 `4px` |
| ミュウのアメ枠 | 最小高さ `88px`を継承、内側上下 `8px`・左右 `12px`、内部要素間 `4px` |
| ミュウの数量ボタン列 | 上余白 `4px`、ボタン間 `8px` |
| ミュウの数量ボタン | 最小幅・最小高さともに `44px` |
| 通常ボタン | 最小高さ `44px`、太さ `600` |
| 保存・記録の送信ボタン | 最小高さ `48px`、内側上下 `12px`・左右 `16px` |
| 入力欄 | 最小高さ `48px`、内側上下 `10px`・左右 `12px` |
| 入力ラベル | 上下余白 `12px`。入力上余白は共通CSSの `6px` |
| 2列入力フォーム | 列間 `12px`、行間の追加gap `0` |
| 日時の帯 | 内側上下 `8px`・左右 `0`、日時とラベルの間隔 `8px` |
| 日時入力・ボタン | 高さ／最小高さ `48px`、ボタン左右内側余白 `8px` |
| 「今すぐ記録」全体 | 最小幅 `100px`、最小高さ `48px`、内側上下 `4px`・左右 `6px` |
| 「今すぐ記録」内部 | チェック部分 `18px × 18px`、文字との間隔 `6px` |
| 折りたたみカード | 左右内側余白 `16px`、開いた場合のみ下 `16px` |
| 折りたたみ見出し | `16px / 600`、最小高さ `56px`、上下内側余白 `16px`、行高 `24px` |

セクション間の見える距離は、隣り合う要素のmarginの相殺にも影響される。
上記の上下余白を単純に足した値を、実測の間隔として扱わない。

### 15.5 共通部分との境界・現在残っている例外

- `record-design.css` は `#record` 内に限定されている。
  ページ背景・アプリ名のヘッダー・下部タブは、現在も `style.css` の共通設定を使用する。
- 第6節の左右16pxは目安であり、現在の390px幅では左右12pxである。
  第4節の画面タイトル22pxも目安であり、共通ヘッダーのアプリ名は現在27pxである。
- 日時選択パネルは `#record` の外に生成されるため、上記の記録タブ専用設定は適用されない。
  現状は共通 `.record-picker` の背景 `#fffefa`、角丸 `22px`、内側余白 `20px`、
  幅 `min(440px, calc(100% - 24px))` を使用する。
  時間・分の選択欄は高さ `68px`、文字 `30px / 700`。
  画面高さ650px以下ではパネル内側14px、時間・分の高さ56pxとなる。
  この例外を通常カードや他タブの新しい標準値として増やさない。
- 今回はこれらの差異を記録しただけで、HTML・CSS・JavaScript・機能仕様は変更していない。
