# メインスキルマスター

一覧と種族別の初期値は `main-skill-master.js` にまとめています。

- 名称変更：`entries` の該当項目の `name` だけを変更します。
- スキル追加：重複しない `id`、表示名 `name`、保存用の `legacyName` を持つ項目を追加します。
- ミュウの選択肢：対象項目の `mew: true` で管理します。
- ポケモンの基本スキル：`species` の `skillId` で指定します。
- かけら獲得の種類：種族の `shardMode`（fixed／random／lucky）で管理します。獲得量の計算は既存の core.js にあります。

`id` と `legacyName` は保存済みデータとの対応に使うため変更しません。旧表記を追加する場合は `aliases` に記入します。スキルの動作を新設する場合は、名前の追加とは別に記録処理の実装が必要です。

登録時に基本スキルを選択し、選ばれたスキルを個体に保存します。既存個体の選択は優先し、未保存の個体は基本スキルを表示上だけ補います。過去の履歴は現在の個体設定ではなく、記録に保存された名前やIDをマスターで表示します。

基本スキルの出典：[ポケモンの一覧](https://wikiwiki.jp/poke_sleep/ポケモンの一覧)（2026年9月15日確認）。ミュウの選択肢と従来のかけら対象の名前・扱いは、アプリの指定仕様を優先しています。

GitHub Pagesに反映するときは、この新しいJSファイルもindex.htmlと同じ場所へ配置してください。
